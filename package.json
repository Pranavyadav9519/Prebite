{
  "name": "prebite-monorepo",
  "version": "1.0.0",
  "description": "Prebite Monorepo",
  "private": true,
  "scripts": {
    "build:frontend": "cd frontend && npm install && npm run build",
    "build:backend": "cd backend && npm install && npx prisma generate",
    "vercel-build": "npm run build:frontend && npm run build:backend"
  }
}
